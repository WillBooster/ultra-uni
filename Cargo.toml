[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[profile.release]
lto = true
codegen-units = 1
# The wasm binary is shipped inside Cloudflare Workers bundles, whose size is capped.
opt-level = "s"
